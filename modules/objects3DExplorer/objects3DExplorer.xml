<module>
    <!-- module's name should match its executable file's name. -->
    <name>objects3DExplorer</name>
    <description> Uses depth images to build 3D pointclouds from the object from different perspectives, and merges it to get a complete model.</description>
    <version>1.0</version>

    <!-- <arguments> can have multiple <param> tags-->
    <arguments>
        <param desc="Name of the module" default="toolExplorer"> name</param>
        <param desc="Sub-path from \c $ICUB_ROOT/app to the configuration file" default="toolModeler"> context </param>
    </arguments>

    <!-- <authors> can have multiple <author> tags. -->
    <authors>
	  <author email="tanis.mar@iit.it"> Tanis Mar</author>
    </authors>

     <!-- <data> can have multiple <input> or <output> tags. -->
     <data>
        <!-- input data if available -->
        <input port_type="service">
            <type>rpc</type>
            <port carrier="tcp">/objects3DExplorer/rpc:i</port>
            <description>takes RPC commands from Human or stateMachine</description>
        </input>      
        <input>
            <type>Bottle</type>
            <port carrier="tcp">/objects3DExplorer/clouds:i</port>
            <description>Receives clouds in Bottles, as BottleOf(lists), where each list is a 3 value XYZ point </description>
        </input>   
        <!-- output data if available -->        
        <output port_type="service">
            <type>rpc</type>
            <port>/objects3DExplorer/clouds:o</port>
            <description> Send out the latest reconstructed mesh, for visualization or further processing</description>
        </output>
        <output port_type="service">
            <type>rpc</type>
            <port>/objects3DExplorer/objrec:rpc</port>
            <description> Send commands to the objectReconstruction module</description>
        </output>
        <output port_type="service">
            <type>rpc</type>
            <port>/objects3DExplorer/visualizer:rpc</port>
            <description> Send commands to the visualizer module to display the pointcloud</description>
        </output>
        <output port_type="service">
            <type>rpc</type>
            <port>/objects3DExplorer/featExt:rpc</port>
            <description> Send commands to the toolFeatExt module to extract 3D geometrical features from the tool</description>
        </output>        

    </data>

    <dependencies>
        <computer>
        </computer>
    </dependencies>

    <!-- specific libraries or header files which are used for development -->
    <development>
        <header></header>
        <library>YARP</library>
        <library>icubmod</library>
        <library>ctrlLib</library>
        <library>PCL 1.7</library>
    </development>

</module>

