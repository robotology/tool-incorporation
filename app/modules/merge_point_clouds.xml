<module>
    <!-- module's name should match its executable file's name. -->
    <name>merge_point_clouds</name>
    <description> A module that uses ICL to merge point clouds of the same object obtaiend from different views</description>
    <version>1.0</version>

    <!-- <arguments> can have multiple <param> tags-->
    <arguments>
        <param desc="Name of the module" default="merge_point_clouds"> name</param>
	<param desc="Robot" default="icub"> robot</param>
        <param desc="Sub-path from \c $ICUB_ROOT/app to the configuration file" default="grasp"> context </param>
    </arguments>

    <!-- <authors> can have multiple <author> tags. -->
    <authors>
	  <author email="ilaria.gori@iit.it"> Tanis Mar</author>
    </authors>

     <!-- <data> can have multiple <input> or <output> tags. -->
     <data>
        <!-- input data if available -->
        <input port_type="service">
            <type>rpc</type>
            <port carrier="tcp">/mergeModule</port>
            <description>takes RPC commands from Human or stateMachine</description>
        </input>
    </data>

    <dependencies>
        <computer>
        </computer>
    </dependencies>

    <!-- specific libraries or header files which are used for development -->
    <development>
        <header></header>
        <library>YARP</library>
        <library>PCL 1.7</library>
    </development>

</module>

